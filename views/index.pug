extends template/layout
block body
  h1 Hello!
  p.
    This is an example of a website that uses Express, Typescript (powered with esbuild), Jest and Prisma.
  if !loggedAccount
    a(href="/register") Register a new user
    p or
    a(href="/login") Log in
  else
    p= `Welcome, ${loggedAccount}`